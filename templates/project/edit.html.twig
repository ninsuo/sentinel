{% extends 'project/_layout.html.twig' %}

{% set breadcrumb_items = [
    { label: 'Configuration' }
] %}

{% block title %}Edit {{ project.name }} · Sentinel{% endblock %}

{% block project_content %}
    <div class="card shadow-sm">
        <div class="card-header bg-white fw-semibold">Edit project</div>
        <div class="card-body">
            {% include 'project/_form.html.twig' with { form: form, submit_label: 'Save' } %}
        </div>
    </div>
{% endblock %}
