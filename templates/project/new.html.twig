{% extends 'base.html.twig' %}

{% block title %}New project · Sentinel{% endblock %}

{% block body %}
    <div class="card shadow-sm">
        <div class="card-header bg-white fw-semibold">New project</div>
        <div class="card-body">
            {% include 'project/_form.html.twig' with { form: form, submit_label: 'Create' } %}
        </div>
    </div>
{% endblock %}
