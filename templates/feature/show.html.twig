{% extends 'project/_layout.html.twig' %}

{% set breadcrumb_items = [
    { label: 'Feature: ' ~ feature.name }
] %}

{% block project_content %}
    <div class="p-4 bg-white border rounded-3 shadow-sm mb-3">
        <div class="d-flex justify-content-between align-items-start gap-3">
            <div>
                <h1 class="h4 fw-semibold mb-1">{{ feature.name }}</h1>
                <div class="text-secondary small">Feature prompt</div>
            </div>

            <form method="post" action="{{ path('app_feature_run', {projectId: project.id, featureId: feature.id}) }}">
                <input type="hidden" name="_token" value="{{ csrf_token('run_feature_' ~ feature.id) }}">
                <button class="btn btn-primary">Run</button>
            </form>
        </div>

        <hr>

        <pre class="mb-0 small text-break" style="white-space: pre-wrap;">{{ feature.prompt }}</pre>
    </div>

    <div class="card shadow-sm">
        <div class="card-header bg-white fw-semibold">Runs</div>

        <div class="table-responsive">
            <table class="table mb-0 align-middle">
                <thead class="table-light">
                <tr>
                    <th>ID</th>
                    <th>Created</th>
                    <th>Status</th>
                    <th>Duration</th>
                </tr>
                </thead>
                <tbody>
                {% if runs is empty %}
                    <tr>
                        <td colspan="4" class="text-secondary text-center py-5">No runs yet. Press “Run”.</td>
                    </tr>
                {% else %}
                    {% for run in runs %}
                        <tr>
                            <td class="fw-semibold">#{{ run.id }}</td>
                            <td class="text-secondary small">{{ run.createdAt|date('Y-m-d H:i') }}</td>
                            <td><span class="badge text-bg-light border">{{ run.status ?? '-' }}</span></td>
                            <td class="text-secondary small">{{ run.durationMs ? (run.durationMs ~ ' ms') : '-' }}</td>
                        </tr>
                    {% endfor %}
                {% endif %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
